Test project testing any services and scripts created on the implemenation
Assumes a connection to a running content hub instance, but tests could be created to be fully offline, using Xunit and Moq
This project is executable, which means it makes use of the following config files:
	appsettings.json
	local.settings.json
	user secrets

This project can access services from all projects